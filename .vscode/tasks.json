{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Pull Kivy Submodule",
            "type": "shell",
            "command": "cd ${workspaceFolder}/stubs/kivy && git pull origin main && cd ${workspaceFolder}",
            "problemMatcher": []
        }
    ]
}